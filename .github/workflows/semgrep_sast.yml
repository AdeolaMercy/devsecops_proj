name: Semgrep SAST Scan

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  semgrep:
    name: Run Semgrep SAST
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Semgrep
        run: |
          pip install semgrep
        working-directory: juice_shop

      - name: Run Semgrep scan
        run: |
          semgrep --config=auto --exclude node_modules
        working-directory: juice_shop

      # OPTIONAL: Upload report as artifact
      - name: Save Semgrep Report
        run: semgrep --config=auto --json --exclude node_modules > semgrep-report.json
        working-directory: juice_shop

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: juice_shop/semgrep-report.json

      - name: Run Semgrep Cloud Scan
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: semgrep ci
